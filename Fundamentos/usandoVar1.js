{{{{ var sera = "Será???? ";  }}}}
console.log(sera)

function teste() {
    var local = 123
    console.log(local)
}

teste()
console.log(local)

/**
 * Variáveis criadas em bloco de instruções, podem ser acessadas externamente.
 * Variáveis criadas dentro de uma função não podem ser utilizadas de forma global.
 * Evitar utilizar variáveis globais em JavaScript.
 */
